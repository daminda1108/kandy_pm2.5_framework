# ─────────────────────────────────────────────────────────────
# Kandy PM2.5 Dual-Track Research Project
# requirements.txt
# ─────────────────────────────────────────────────────────────

# ── Core scientific stack ─────────────────────────────────────
numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3

# ── ML Models (Stage 1) ───────────────────────────────────────
xgboost>=2.0
lightgbm>=4.0
shap>=0.44          # Interpretability

# ── Deep Learning (Stage 2 PINN) ──────────────────────────────
torch>=2.1

# ── Geospatial / Remote Sensing ───────────────────────────────
rasterio>=1.3
xarray>=2023.6
netCDF4>=1.6
h5py>=3.9
pyproj>=3.6
geopandas>=0.14
shapely>=2.0
fiona>=1.9
richdem>=2.3.4      # DEM terrain analysis (TPI, slope, aspect)
osmnx               # Street networks from OpenStreetMap

# ── Satellite Data Access ─────────────────────────────────────
earthaccess>=0.8    # NASA Earthdata (MODIS downloads)
cdsapi>=0.6         # Copernicus CDS (ERA5 downloads)
sentinelsat>=1.2    # Sentinel / TROPOMI downloads
earthengine-api     # Google Earth Engine API

# ── Data Visualisation ────────────────────────────────────────
matplotlib>=3.7
cartopy>=0.22       # Geospatial map projections
plotly>=5.17        # Interactive visualizations
seaborn>=0.13
contextily>=1.4.0   # Basemap tiles for geospatial plots
folium>=0.15.0      # Interactive HTML maps (station/sensor visualisation)
python-docx>=1.1.0  # Supervisor report generation (Word .docx)

# ── Utilities ────────────────────────────────────────────────
tqdm>=4.66          # Progress bars
pyyaml>=6.0         # Config files
joblib>=1.3         # Parallel processing
optuna>=3.4         # Hyperparameter optimization
requests>=2.31
python-dateutil>=2.8

# ── Notebook support ──────────────────────────────────────────
jupyter>=1.0
ipywidgets>=8.1
